[project]
name = "vt_automator"
version = "0.1.0"
dependencies = ["flask"]

[project.scripts]
vt-automator = "vt_automator.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
