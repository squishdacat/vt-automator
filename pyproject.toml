[project]
name = "vt_automator"
version = "0.1.0"
dependencies = [
    "flask",
    "gunicorn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["vt_automator"]
